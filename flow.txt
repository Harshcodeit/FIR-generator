Citizen Audio
↓
Whisper
↓
Transcript
↓
Gemma (Extraction)
↓
Structured FIR JSON
↓
Missing Field Checker
↓
User Follow-up Questions
↓
Complete JSON
↓
RAG (BNS + BNSS + FIR Manual)
↓
Relevant Legal Sections
↓
Gemma (FIR Generation)
↓
Verification Layer
↓
PDF Generator
↓
Final FIR


extract_service
        │
        ▼
partial report
        │
        ▼
question_service
        │
        ├── next question
        ├── update report
        ├── next question
        └── ...
        │
        ▼
complete report
        │
        ▼
reasoning_service
        │
        ▼
generation_service

Ingestion flow:
loaders → chunking → embeddings → vector_store

query → embeddings → vector_store → results


chunking 
Legal document
       │
       ▼
Regex Splitter
       │
       ▼
Section 378

Section 379

Section 380
       │
       ▼
Check size
       │
 ┌─────┴────────┐
 │              │
Small        Huge
 │              │
Keep        Recursive Splitter
 │              │
 └─────┬────────┘
       ▼
Final chunks
       │
       ▼
Embeddings

regex lets you describe text patterns instead of searching for exact strings

retrieve a complete provision like Section 307 instead of fragments from unrelated sections. Only exceptionally long sections are split, which keeps retrieval accurate while staying within embedding size limits. This is a much better fit for legal RAG than relying on a character-based splitter alone.

the pipeline

Incident (str)
        │
        ▼
Extraction
        │
        ▼
UserReportSchema
        │
        ▼
Question Service (loop)
        │
        ▼
Completed UserReportSchema ──────────────|
        |                                |
        │              │                 |
        ▼              ▼                 ▼
Analysis (RAG)     FIR Template      User report    
        │              │                 |
        └──────┬───────┘──────────────────
               ▼
         Generation





In choosing a gemma 4 version, tradeoff between capability and cost of processing cycles,m/y cost and power consumptions

Size - E2B(E stands for effective),E4B,12B,A4B 26B,31B

E4B->reasoning,multimodal,increased context window-128k tokkne
desc - 4.5B paramters,42 layers

use case - text generation & summarization,chatbots,image data extraction,research

limitations
training data - quality and diversity of training data influence model's capability
context and task complexity-opened ended or complex tasks can be challening
lack of common sense

__init__ tells python to treat dir as package


gemma4:26b mix of experts uses 26b paramters but only 4b are active at a time

importing a model executes it immediately in python 